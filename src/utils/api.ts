/**
 * Boros Open API base URL. Same host the new `@pendle/boros-sdk-public`
 * SDK targets by default — kept as a constant here so the "direct API"
 * version of each example doesn't have to repeat it.
 */
export const API_BASE_URL = "https://api-boros.pendle.finance/apis";
